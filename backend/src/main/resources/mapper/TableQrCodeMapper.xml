<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.unki11.bestorder.table.repository.TableQrCodeRepository">

    <resultMap id="TableQrCodeResultMap" type="com.unki11.bestorder.table.domain.TableQrCode">
        <id property="qrId" column="qr_id" />
        <result property="storeId" column="store_id" />
        <result property="tableId" column="table_id" />
        <result property="qrToken" column="qr_token" />
        <result property="qrUrl" column="qr_url" />
        <result property="qrImageData" column="qr_image_data" />
        <result property="status" column="status" />
        <result property="useYn" column="use_yn" />
        <result property="expiresAt" column="expires_at" />
        <result property="createdAt" column="created_at" />
        <result property="createdUser" column="created_user" />
        <result property="updatedAt" column="updated_at" />
        <result property="updatedUser" column="updated_user" />
    </resultMap>

    <sql id="tableQrCodeColumns">
        qr_id, store_id, table_id, qr_token, qr_url, qr_image_data,
        status, use_yn, expires_at,
        created_at, created_user, updated_at, updated_user
    </sql>

    <insert id="insertTableQrCode" parameterType="com.unki11.bestorder.table.domain.TableQrCode" useGeneratedKeys="true" keyProperty="qrId">
        INSERT INTO table_qr_codes (
        store_id, table_id, qr_token, qr_url, qr_image_data,
        status, use_yn, expires_at,
        created_at, created_user
        ) VALUES (
        #{storeId}, #{tableId}, #{qrToken}, #{qrUrl}, #{qrImageData},
        'ACTIVE', 'Y', #{expiresAt},
        NOW(), #{createdUser}
        )
    </insert>

    <select id="selectQrCodeByStoreAndToken" resultMap="TableQrCodeResultMap">
        SELECT
        <include refid="tableQrCodeColumns" />
        FROM table_qr_codes
        WHERE qr_token = #{qrToken}
        AND use_yn = 'Y'
        ORDER BY created_at DESC
        LIMIT 1
    </select>

    <select id="selectQrCodeByStoreAndTable" resultMap="TableQrCodeResultMap">
        SELECT
        <include refid="tableQrCodeColumns" />
        FROM table_qr_codes
        WHERE store_id = #{storeId}
        AND table_id = #{tableId}
        AND use_yn = 'Y'
        ORDER BY created_at DESC
        LIMIT 1
    </select>

    <update id="updateTableQrCodeStatus" parameterType="com.unki11.bestorder.table.domain.TableQrCode">
        UPDATE table_qr_codes
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="useYn != null">use_yn = #{useYn},</if>
            <if test="expiresAt != null">expires_at = #{expiresAt},</if>
            <if test="qrImageData != null">qr_image_data = #{qrImageData},</if>
            updated_at = NOW(),
            updated_user = #{updatedUser}
        </set>
        WHERE store_id = #{storeId}
        AND table_id = #{tableId}
        AND qr_id = #{qrId} </update>

    <select id="selectAllQrCodesByStoreId" parameterType="long" resultMap="TableQrCodeResultMap">
        SELECT
        <include refid="tableQrCodeColumns" />
        FROM table_qr_codes
        WHERE store_id = #{storeId}
        AND use_yn = 'Y'
        ORDER BY table_id ASC, created_at DESC
    </select>

</mapper>